#ifndef scrolled_widgets_h
#define scrolled_widgets_h

#include <wx/wx.h>

class ScrolledWidgets : public wxScrolledWindow {
	private:

	public:
	ScrolledWidgets(wxWindow* parent, wxWindowID id);

};

#endif